# Orpheus TTS streaming server configuration
# NOTE: Use KEY=VALUE lines (no `export`). The server reads this file at startup.

ORPHEUS_MODEL=canopylabs/orpheus-tts-0.1-finetune-prod
MAX_MODEL_LEN=2048
GPU_MEMORY_UTILIZATION=0.9
HOST=0.0.0.0
PORT=8080

